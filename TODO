* registering an account
	* notifications when new versions change your output (or when they don't too maybe?)
* nightly builds
* webgrind output, valgrind

* my-favorites
* specify majors, [] > 5.1, [] > 5.2.*

premium: (25 $ / m)
	personal subdomain / script submitted by ... on ..

phar.hosting with single selectable version and HTML output. Interactive, emulate fastcgi

"navigable tree of history, so like your "based on xxxxx" thing but showing a complete tree back to the initial paste"

move version. options from html to js
don't move preview in js, use html for quicker rendering
[whitelisted] referal logging
