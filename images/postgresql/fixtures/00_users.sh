#!/bin/bash

psql -U ${POSTGRES_USER} <<-END
    CREATE ROLE ${POSTGRES_USER_DAEMON} LOGIN PASSWORD '${POSTGRES_PASSWORD_DAEMON}';
    CREATE ROLE ${POSTGRES_USER_WEBSITE} LOGIN PASSWORD '${POSTGRES_PASSWORD_WEBSITE}';
END
