{$r = $this->input->getSegfault();}

{$this->output = $r->getSingle()->output->getRaw($this->input, 'segfault');}
{$banner = '*** Segmentation fault';}
{$offset = strrpos($this->output, $banner) + strlen($banner);}
{$this->output = substr($this->output, $offset);}

"segfault": {output}
